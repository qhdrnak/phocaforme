import { Container } from '@mui/material';

const payment = () => {
    return (
        <Container>
            <h1 className="post-page-title">결제페이지</h1>
        </Container>
    );
};

export default payment;
