import { Container } from "@mui/material";
import ChatList from "../components/ChatRoom/ChatList";

const chat = () => {
    return (
        <Container>
            <ChatList/>
        </Container>
    );
};

export default chat;