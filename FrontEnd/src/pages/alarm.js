//알림페이지
import AlarmList from '../components/Alarm/AlarmList';

const alarm = () => {
    return (
        <div id='alarm-page'>
            <AlarmList/>
        </div>
    );
};

export default alarm;