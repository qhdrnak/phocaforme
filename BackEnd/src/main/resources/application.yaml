spring:
  config:
    import:
      - optional:file:.env[.properties]
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://kofoos.c16mgeqgsinu.ap-northeast-2.rds.amazonaws.com:3306/kofoos?serverTimezone=UTC&characterEncoding=UTF-8
    username: admin
    password: zhvntmA309
  # datasource:
  #   url: ${DB_URL}
  #   username: ${USER_NAME}
  #   password: ${USER_PASSWORD}
  #   driver-class-name: org.mariadb.jdbc.Driver
