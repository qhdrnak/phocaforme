# 배포시
spring:
  config:
    import:
      - optional:file:.env[.properties]
      - classpath:/application-global.yml
      - classpath:/application-oauth-global.yml
      - classpath:/application-aws.yml
      - classpath:/application-elastic-global.yml
      - classpath:/application-rabbit-global.yml
  profiles:
    active: global, oath-global, aws, rabbit-global, elastic-global

## 로컬시
#spring:
#  config:
#    import:
#      - optional:file:.env[.properties]
#      - classpath:/application-aws.yml
#      - classpath:/application-local.yml
#      - classpath:/application-oauth-local.yml
#      - classpath:/application-elastic-local.yml
#      - classpath:/application-rabbit-local.yml
#  profiles:
#    active: local, oauth-local, aws, rabbit-local, elastic-local