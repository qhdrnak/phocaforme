spring:
  config:
    import:
      - optional:file:.env[.properties]
      - classpath:/application-ssafy-dev.yml
      - classpath:/application-oauth.yml
  profiles:
    active: global, ssafy, oauth, aws